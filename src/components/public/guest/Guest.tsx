/* import { Typography } from '@mui/material';
import React from 'react';
import { Outlet } from 'react-router-dom';

const Guest = () => {
	return (
		<div>
			<Typography variant='body1'>
				Puedes registrarte y comenzar a crear y
				compartir tus examenes.
			</Typography>
			<div>
				<Outlet />
			</div>
		</div>
	);
};

export default Guest;
 */
